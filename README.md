# code_glue

small python script that allows to glue source files into one file

## Usage

```bash
python script.py <output_file> <prepend_file> <list_file>
```

`output file` = where result goes

`prepend file` = prepend of result

`list file` = list of source files and directories to glue
